#include <stdio.h>

int main() {
    int i;

    printf("Número\tQuadrado\tCubo\n");
    for(i = 0; i <= 10; i++) {
        printf("%d\t%d\t\t%d\n", i, i*i, i*i*i);
    }

    return 0;
}

o /t é usada para tabulação
